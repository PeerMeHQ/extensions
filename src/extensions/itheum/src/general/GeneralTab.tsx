import React from 'react'
import { _ClaimsSection } from './_ClaimsSection'

export const GeneralTab = (props: { app: any }) => {
  return (
    <>
      <_ClaimsSection app={props.app} />
    </>
  )
}
